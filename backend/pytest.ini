[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: menjalankan OCR sungguhan (CPU-bound, puluhan detik per halaman).
        Lewati saat iterasi cepat:  pytest backend/tests -m "not slow"
