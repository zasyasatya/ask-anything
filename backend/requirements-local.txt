# Inference HuggingFace **lokal** (provider `huggingface`, hf_mode=local).
#
# Dipisah dari requirements.txt karena PyTorch berukuran ratusan MB dan tidak
# dibutuhkan untuk deployment yang memakai API (OpenAI / gateway) atau mock.
# Install:  .venv/bin/pip install -r backend/requirements-local.txt
# CPU saja: .venv/bin/pip install -r backend/requirements-local.txt \
#               --extra-index-url https://download.pytorch.org/whl/cpu
#
# Tanpa paket ini backend tetap jalan; UI menampilkan petunjuk install dan mode
# openai/mock tetap bisa dipakai.
# torch>=2.2
# transformers>=4.46
# accelerate>=1.0
# safetensors>=0.45
# sentencepiece>=0.2
# huggingface_hub>=0.25

torch>=2.2
transformers>=4.46
accelerate>=1.0
safetensors==0.8.0
sentencepiece>=0.2
huggingface_hub>=0.25


